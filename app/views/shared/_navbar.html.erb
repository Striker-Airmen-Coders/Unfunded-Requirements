<nav class="flex flex-row items-center px-2 justify-between bg-nord5 text-nord0">
  <a href=/reqs >
    <pre class="text-xs font-black text-nord0 px-2 pt-2">
  / _ \
\_\(_)/_/
 _//"\\_  
  /   \
    </pre>
  </a>
  <h1 class="line-clamp-1"><%= @title %></h1>
  <div>
  <% if user_signed_in? %>
    <div class="md:pr-2">
    <% if current_user.fma_role == true || current_user.hq_role == true %> 
      <%= link_to 'Admin Panel', rails_admin_path, class: 'hidden sm:inline' %> 
      <strong class="hidden sm:inline"> | </strong>
    <% end %> 
    <%= link_to 'Logout', destroy_user_session_path, :method => :delete %></div>   
  <% else %>
    <%= link_to 'Sign In', new_user_session_path %> </div>
  <% end %>
  </div>
</nav>
<h2 class="notice bg-nord5 text-nord0 text-center"><%= notice %></h2>
<h2 class="alert bg-nord5 text-nord0 text-center"><%= alert %></h2>
